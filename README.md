# GanaNet

Login de banca digital inspirado en la experiencia de Banco Ganadero.

## Desarrollo

```bash
npm install
npm run dev
```

Abre la URL que muestre Vite (por defecto `http://localhost:5173`).
